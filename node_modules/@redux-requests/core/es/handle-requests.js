import _extends from "@babel/runtime/helpers/extends";
import { requestsReducer } from './reducers';
import { createRequestsCacheMiddleware, createClientSsrMiddleware, createServerSsrMiddleware, createSendRequestsMiddleware } from './middleware';
import defaultConfig from './default-config';

var defer = function defer() {
  var res;
  var rej;
  var promise = new Promise(function (resolve, reject) {
    res = resolve;
    rej = reject;
  });
  promise.resolve = res;
  promise.reject = rej;
  return promise;
};

var handleRequests = function handleRequests(userConfig) {
  var config = _extends({}, defaultConfig, userConfig);

  var requestsPromise = config.ssr === 'server' ? defer() : null;
  return {
    requestsReducer: requestsReducer(config),
    requestsMiddleware: [config.ssr === 'server' && createServerSsrMiddleware(requestsPromise, config), config.ssr === 'client' && createClientSsrMiddleware(config), config.cache && createRequestsCacheMiddleware(config), createSendRequestsMiddleware(config)].filter(Boolean),
    requestsPromise: requestsPromise
  };
};

export default handleRequests;